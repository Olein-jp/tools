# 作業ログ

## 2026-03-26

- 既存の `React + Vite` 構成、ルーティング、ツール一覧、SEO 定義、既存 deploy workflow を確認した
- `docs/2026-03-26_簡易ビジュアルリグレッションテスト実装メモ.md` を追加し、実装方針とデプロイ見直し方針を記録した
- 簡易ビジュアルリグレッションテストツールの新規ページ追加に着手した
- `pixelmatch` を導入し、Canvas API ベースでブラウザ内比較を行う前提を整えた
- Before/After のアップロード、ドラッグ＆ドロップ、差分ハイライト表示、エラー表示、リセット、再比較に対応する実装を進めた
- GitHub Actions のデプロイを FTP から SSH ベースへ見直す対応を進めた
- `npm run build` を実行し、新規ページ追加後も本番ビルドが通ることを確認した
- 画像比較条件を「同一解像度」から「横幅一致のみ必須」へ変更し、縦幅が異なる画像でも比較できるように調整した
- GitHub Actions の deploy 失敗ログを確認し、`XSERVER_*` secrets が空のまま展開されると `ssh-keyscan` が `Bad port ''` で失敗することを整理した
- `.github/workflows/deploy.yml` に deploy 用 secrets の事前検証ステップを追加し、未設定の secret 名がログに出るよう改善した
- `docs/2026-03-26_GitHub_Actions_デプロイ設定確認.md` を追加し、原因、確認項目、再発防止内容を記録した
- `docs/2026-03-26_GitHub_Actions_シークレット設定サンプル.md` を追加し、`XSERVER_*` secrets の入力例と注意点を整理した
- 過去の `.github/workflows/deploy.yml` の履歴を確認し、以前の GitHub Actions デプロイが `FTP` から `FTPS` を経て、現在の `SSH + rsync` に移行していることを整理した
- `docs/2026-03-26_GitHub_Actions_デプロイ方式変遷確認.md` を追加し、デプロイ方式の変遷と利用していた secret 名の違いを記録した
- ユーザー依頼にあわせて `.github/workflows/deploy.yml` を `SSH + rsync` から `FTPS` ベースへ戻した
- `docs/2026-03-26_GitHub_Actions_FTPS復帰対応.md` を追加し、現在の deploy 設定と必要な `FTP_*` secrets を整理した
- `docs/2026-03-26_GitHub_Actions_シークレット設定サンプル.md` を `FTPS` 前提の内容へ更新し、`XSERVER_*` ではなく `FTP_*` を参照する形へ修正した
- `docs/2026-03-26_GitHub_Actions_デプロイ設定確認.md` に、SSH 構成時点の調査メモである注意書きを追記した

## 記録ルール

- 1日単位で、その日に行った作業内容を追記していく
- 後から見た人が、何を確認し、何を変更したか追える粒度で書く
- `## 記録ルール` は常にこのファイルの最下部に置く
